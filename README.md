# ws-chat

A simple client/server chat implemented in Go using WebSockets.

## Server
To run the server:
```bash
go run .
```

Connect to the server using `websocat`:
```bash
websocat ws://localhost:8080/ws
```
